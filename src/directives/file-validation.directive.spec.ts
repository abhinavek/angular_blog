import { FileValidationDirective } from './file-validation.directive';

describe('FileValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new FileValidationDirective();
    expect(directive).toBeTruthy();
  });
});
